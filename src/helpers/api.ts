export const baseUrl = () => {
  return "http://localhost:4000";
};

export const baseUrlRefPost = () => {
  return `${baseUrl()}/ref/private/account`;
};

export const baseUrlEmployee = () => {
  return `${baseUrl()}/employee/private/account`;
};

export interface refPostType {
  id: number;
  accountName: string;
  accountCode: string;
  accountType: string;
}

export interface generalJournaType {
  id: number;
  period: string;
  description: string;
}
